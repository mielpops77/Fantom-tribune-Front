/* import React from 'react';

class Countries extends React.Component { */
/*     constructor(props) {
        super(props);

        this.state = {
            postId: null
        };
    }
 */
/*     componentDidMount() {
        // Simple POST request with a JSON body using fetch
        const requestOptions = {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ title: 'aaaaaReact POST Request Example' , subTitle: "ce cactus est pas là pour rien", image: "https://cdn.vuetifyjs.com/images/cards/sunshine.jpg",content: "I'm a thing. But, like most politicians, he promised more than he could deliver. You won't have time for sleeping, soldier. not with all the bed"})
        };
        fetch('http://localhost:3000/api/v1/blog-posts', requestOptions)
            .then(response => response.json())
            .then(data => this.setState({ postId: data.id }));
    }

    render() {
        const { postId } = this.state;
        return (
            <div className="card text-center m-3">
                <h5 className="card-header">Simple POST Request</h5>
                <div className="card-body">
                    Returned Id: {postId}
                </div>
            </div>
        );
    } */
/* }

export default Countries; */

